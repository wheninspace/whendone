# Source C — plain solo/TodoWrite jobs (stage 5, not yet shipped)

No declared plan: pace-based ETA from item completion rate, rendered with a visible
"uncalibrated" label; NEVER logged to calibration. The render path already exists
(`render_artifact.py` handles `source: "c"`); the ingester ships in stage 5. Until then,
decline pace-only tailing and offer a declared Source-A plan instead if the user wants an ETA.
